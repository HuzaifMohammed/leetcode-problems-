class Solution {
    public int reverseDegree(String s) {
        int ans = 0;
        char[] ch = s.toCharArray();
        for(int i=0;i<ch.length;i++){
            int val = 26-(ch[i]-'a');
            ans = ans+(val*(i+1));
        }
        return ans;
    }
}
