class Solution {
    public int subtractProductAndSum(int n) {
       int copy = n;
       int mul = 1;
       int sum = 0;
       while(copy!=0){
        int rem = copy%10;
        mul*=rem;
        sum+=rem;
        copy/=10;
       } 
       return mul-sum;
    }
}
