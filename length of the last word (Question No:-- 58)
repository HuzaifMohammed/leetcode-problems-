class Solution {
    public int lengthOfLastWord(String s) {
        s.trim();
        String[] arr = s.split(" ");
        String s1 = arr[arr.length-1];
        int length = s1.length();
        return length;
    }
}
